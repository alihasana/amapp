import React from 'react';
import ContentView from '../../layouts/auth/change-profile';

export const ChangeProfileScreen = ({ navigation }): React.ReactElement => (
  <ContentView navigation={navigation}/>
);
